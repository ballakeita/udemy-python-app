apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: python-app
  description: Python app taken from the Backstage example app.
  # Example for optional annotations
  annotations:
    github.com/project-slug: ballakeita/udemy-python-app
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner:  automation
  lifecycle: experimental
