name: cicd
"on":
  push:
    paths:
      - src/**
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Shorten ID commit
        shell: bash
        run: |
          echo "COMMIT_ID=${GITHUB_SHA::6}" >> "$GITHUB_ENV"
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: "${{ secrets.DOCKERHUB_USERNAME }}"
          password: "${{ secrets.DOCKERHUB_TOKEN }}"
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          push: true
          tags: "papoucha/udemy-python-app:${{ env.COMMIT_ID }}"
    outputs:
      commit_id: ${{ env.COMMIT_ID  }}
  cd:
    needs: ci
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Modify values file
        shell: bash
        run: |
          echo  ${{needs.ci.outputs.commit_id}}
          sudo apt-get update && sudo  apt install python3-xyz
          pip3 install yq
          yq -Yi '.image.tag = "${{needs.ci.outputs.commit_id}}"'  charts/udemy-python-app/values.yaml

      - name: Commit et Push des changements
        uses: EndBug/add-and-commit@v10
        with:
          message: "Update values.yaml with commit ${{needs.ci.outputs.commit_id}}"

      - name: install argocd
        shell: bash
        run: |
          curl -k -sSL -o argocd  https://argocd-server.argocd/download/argocd-linux-amd64
          ls -lh
          chmod +x argocd
          sudo mv ./argocd /usr/local/bin/argocd

      - name: Argocd app  sync
        shell: bash
        run: |
          argocd login argocd-server.argocd  \
          --insecure \
          --grpc-web \
          --username admin  \
          --password ${{ secrets.ARGOCD_PASSWORD}}
      - name: sync app
        run: |
          argocd app  sync python-app
